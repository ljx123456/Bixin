<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:orientation="vertical"
    android:paddingLeft="12dp"
    android:paddingRight="12dp">

    <CheckBox
        android:id="@+id/broker_adds_check"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/broker_adds"
        android:paddingRight="12dp"

        android:button="@null"
        android:drawableRight="@drawable/pay_selector" />

    <com.makeramen.roundedimageview.RoundedImageView
        android:id="@+id/broker_adds_image"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_centerVertical="true"
        android:layout_marginLeft="12dp"
        android:layout_marginRight="6dp"
        android:scaleType="centerCrop"
        app:riv_oval="true" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/broker_adds_image"
        android:orientation="vertical">

        <TextView
            android:id="@+id/broker_adds_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="蜂巢KTV"
            android:layout_marginRight="50dp"
            android:textColor="#ff333333"
            android:textSize="16sp" />

        <RatingBar
            android:id="@+id/broker_adds_rating"
            style="@style/Base.Widget.AppCompat.RatingBar.Small"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:numStars="5"
            android:rating="3.5" />
    </LinearLayout>
</RelativeLayout>